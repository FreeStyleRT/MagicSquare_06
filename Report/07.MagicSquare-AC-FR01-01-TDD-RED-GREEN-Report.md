# MagicSquare AC-FR01-01 TDD RED-GREEN Report

## 작업 개요
- PRD `FR-01` / `AC-FR01-01` 범위로 테스트 계획·RED 테스트·GREEN 최소 구현·결함 목록을 진행했다.
- `docs/test_plan.md`, `defect_list.md`, Boundary/Control 레이어, `tests/boundary/`·`tests/control/`를 추가했다.
- `Report`·`Prompt` 폴더에 본 세션 보고서·Transcript를 export 했다.

## 수정·생성한 파일

| 구분 | 경로 |
|---|---|
| 문서 | `docs/test_plan.md`, `defect_list.md` |
| README | `README.md` (RED 단계 To-Do, 결함 목록 체크) |
| Boundary | `magic_square/boundary/constants.py`, `contracts.py`, `validator.py`, `__init__.py` |
| Control | `magic_square/control/domain_resolver.py`, `resolve_use_case.py`, `__init__.py` |
| 테스트 | `tests/boundary/test_fr01_invalid_size.py`, `tests/control/test_fr01_domain_not_called.py`, `tests/conftest.py`, `tests/helpers/fr01_contract.py` |
| Export | `Report/07.MagicSquare-AC-FR01-01-TDD-RED-GREEN-Report.md`, `Prompt/07.MagicSquare-AC-FR01-01-TDD-Transcript.md` |

## 주요 변경 내용

### 1. 테스트 설계·계획
- PRD 기준 샘플 AC: **AC-FR01-01** (`grid=None` → `INVALID_SIZE` / `Grid must be 4x4.`)
- `docs/test_plan.md`: Track A/B, 경계값, mock/spy, pytest-cov 전략

### 2. RED (실패 테스트)
- Boundary 8건, Control 4건 (Given-When-Then, AC ID 주석, 명명 규칙 준수)
- 초기 실패: `ModuleNotFoundError` (`magic_square.boundary` / `control` 미존재)

### 3. GREEN (최소 구현)
- `BoundaryValidator`: `grid is None`, 4×4 크기만 검증 (AC-FR01-02~05 미포함)
- `ResolveUseCase`: 크기 실패 시 `resolve()` 0회
- `FailureResponse` 계약 단일화 (`tests/helpers` → boundary re-export)

### 4. 결함 관리
- `defect_list.md`: DEF-001 ~ DEF-003 등록·수정 완료
- README 결함 목록 연결 체크박스 완료 처리

## 실행한 명령

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install pytest pydantic pytest-cov pytest-mock
.\.venv\Scripts\python.exe -m pytest tests/boundary/ -v
.\.venv\Scripts\python.exe -m pytest tests/control/ -v
.\.venv\Scripts\python.exe -m pytest tests/boundary/ tests/control/ tests/entity/ -v
git status --short
```

## 명령 결과 요약

| 명령 | 결과 |
|---|---|
| `pytest tests/boundary/` | **8 passed** |
| `pytest tests/control/` | **4 passed** |
| `pytest tests/boundary/ tests/control/ tests/entity/` | **17 passed** |
| 초기 RED (구현 전) | 2 errors — `ModuleNotFoundError` |
| GREEN 직후 (1회) | 1 failed — `FailureResponse` 이중 정의 (`DEF-003`) |

## 테스트 결과

- AC-FR01-01 / AC-FR01-05 관련 **12건** + entity **5건** = **17 passed**
- 커버리지 HTML: `htmlcov/` (로컬 생성 시)

## 결함 요약

| ID | Severity | 상태 |
|---|---|---|
| DEF-001 | Critical | 수정 완료 — boundary 패키지 부재 |
| DEF-002 | Critical | 수정 완료 — control 패키지 부재 |
| DEF-003 | Minor | 수정 완료 — FailureResponse 이중 정의 |

상세: [defect_list.md](../defect_list.md)

## 남은 이슈

- README **RED To-Do** Track A/B·커버리지 항목은 수동 체크 미완료 (테스트는 통과)
- AC-FR01-02~05, FR-02~05는 미구현 (`NotImplementedError` on valid 4×4)
- `requirements-dev.txt` / `pyproject.toml` 미정의

## 다음 작업 제안

1. README RED To-Do Track A/B 항목 체크 갱신
2. AC-FR01-02 RED 테스트 및 GREEN (빈칸 개수 2)
3. Boundary 85%+ / Domain 95%+ gate를 CI에 `--cov-fail-under`로 연결
4. `docs/test_plan.md`와 `defect_list.md` 상호 링크 유지
