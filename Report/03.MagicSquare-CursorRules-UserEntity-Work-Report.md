# MagicSquare Cursor Rules 및 User 엔티티 작업 보고서

작성일: 2026-05-28  
저장소: `MagicSquare_16`  
문서 목적: Cursor Rule 정립부터 ECB 기반 User 엔티티/테스트 생성까지의 수행 내용을 기록한다.

---

## 1. 작업 범위

이번 작업에서는 아래 범위를 수행했다.

1. `.cursorrules` 뼈대 생성
2. `tdd_rules` 상세화
3. `.cursorrules` 전체 섹션 완성
4. ECB 기반 `User` 엔티티 구현
5. `pytest` 기반 테스트 작성 및 실행 검증

---

## 2. 규칙 파일 구성 결과

`/.cursorrules`에 아래 최상위 섹션을 채워 반영했다.

- `project`
- `code_style`
- `architecture`
- `tdd_rules`
- `testing`
- `forbidden`
- `file_structure`
- `ai_behavior`

핵심 반영 사항:

- Python `3.10+`, PEP8, type hints, Google docstring
- ECB 레이어 책임 및 의존 방향 제약
- RED/GREEN/REFACTOR 단계별 `rules` / `must_not`
- pytest + AAA, 커버리지 최소 `80%`
- 금지 규칙: `print()`, 하드코딩 상수, bare `except`

---

## 3. 코드 및 테스트 산출물

생성 파일:

- `magic_square/__init__.py`
- `magic_square/entity/__init__.py`
- `magic_square/entity/user.py`
- `tests/entity/test_user.py`

`User` 엔티티 구현 포인트:

- 불변 데이터 모델(`dataclass(frozen=True, slots=True)`)
- 생성 시 `user_id`, `name`, `email` 검증
- `change_email()`로 새 인스턴스 반환
- 모든 public 메서드에 Google 스타일 docstring 적용

테스트 포인트:

- 유효값 생성 성공
- 빈 이름 예외
- 잘못된 이메일 예외
- 이메일 변경 시 새 객체 반환
- 이메일 변경 실패 예외

---

## 4. 검증 결과

실행 명령:

- `python -m pytest -q`

결과:

- `5 passed in 0.03s`
- 린터 진단: 변경 파일 기준 오류 없음

---

## 5. 산출물 Export

요청에 따라 현재까지 대화형 프롬프트 transcript를 `Prompt` 폴더로 Export한다.

